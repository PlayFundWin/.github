{
  "//": "Copy to .mcp.json and supply values via environment variables. NEVER commit real secrets.",
  "mcpServers": {
    "aws": {
      "//": "Official AWS MCP servers (awslabs). Add the specific servers you need; CloudWatch is the priority for the outage.",
      "command": "uvx",
      "args": ["awslabs.cloudwatch-mcp-server@latest"],
      "env": {
        "AWS_REGION": "${PFW_AWS_REGION}",
        "AWS_ACCESS_KEY_ID": "${PFW_OPS_AWS_KEY}",
        "AWS_SECRET_ACCESS_KEY": "${PFW_OPS_AWS_SECRET}"
      }
    },
    "pfw-postgres": {
      "//": "Read-only Postgres user. Reachable only from inside the VPC (ops box) or via SSM port-forward.",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-postgres", "${PFW_PG_READONLY_URL}"]
    },
    "pfw-mysql": {
      "//": "Read-only MySQL user. Same VPC/tunnel reachability constraint as Postgres.",
      "command": "npx",
      "args": ["-y", "@benborla29/mcp-server-mysql"],
      "env": {
        "MYSQL_HOST": "${PFW_MYSQL_HOST}",
        "MYSQL_PORT": "3306",
        "MYSQL_USER": "${PFW_MYSQL_RO_USER}",
        "MYSQL_PASS": "${PFW_MYSQL_RO_PASS}",
        "MYSQL_DB": "${PFW_MYSQL_DB}",
        "ALLOW_INSERT_OPERATION": "false",
        "ALLOW_UPDATE_OPERATION": "false",
        "ALLOW_DELETE_OPERATION": "false"
      }
    }
  }
}
